let visitante = {nome:"Cauã" , idade:20 , documento:"sim"}

function msg(){
  console.log(`Visitante${visitante.nome}, com ${visitante.idade} anos,portador do documento ${visitante.documento}`)
}

msg(visitante)